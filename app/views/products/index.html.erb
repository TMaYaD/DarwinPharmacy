<% content_for :head do %>
  <script>
    var sColumns = ['id', 'name', 'manufacturer', 'schedule', 'composition', 'dosage_form', 'strength', 'common_uses']
    var tName = 'product'
  </script>
<% end %>

<% table_for @products, :html => { :class => 'data' } do |t| %>
    <%= t.data :id, :name, :manufacturer, :schedule, :composition, :dosage_form, :strength, :common_uses %>
<% end %>

<%= link_to 'New product', new_product_path, :id => 'add_new', :class => 'ui-icon ui-icon-plusthick' %>
