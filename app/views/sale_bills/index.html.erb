<% content_for :head do %>
  <script>
    var sColumns = [ 'sale_bills.id', 'franchises.name', 'sale_bills.created_at' ]
    var tName = 'sale_bill'
  </script>
<% end %>

<% table_for @sale_bills, :html => { :class => 'data' } do |t| %>
  <%= t.data :id, :franchise, :created_at %>
<% end %>

<%= link_to 'New sale_bill', new_sale_bill_path, :id => 'add_new', :class => 'ui-icon ui-icon-plusthick' %>
