<% f.object.product_batch ||= ProductBatch.new %>
<% f.semantic_fields_for :product_batch do |builder| %>
  <%= render 'product_batch_inputs', :f => builder %>
<% end %>
<%= f.input :sale_quantity %>
<%= f.input :free_quantity %>
<%= f.input :discount %>
<%= f.input :rate %>
<%= f.input :vat, :as => :select, :collection => PurchaseBillItem::VATS, :label_method => Proc.new{ |a| a.to_s + " %" } %>
<%= link_to_remove_fieldset "remove", f %>
