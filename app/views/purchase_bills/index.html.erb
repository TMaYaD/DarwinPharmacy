<% content_for :head do %>
  <script>
    var sColumns = [ 'purchase_bills.id', 'bill_number', 'suppliers.name', 'due_date' ]
    var tName = 'purchase_bill'
  </script>
<% end %>

<% table_for @purchase_bills, :html => { :class => 'data' } do |t| %>
  <%= t.data :id, :bill_number, :supplier, :due_date %>
<% end %>

<%= link_to 'New purchase_bill', new_purchase_bill_path, :id => 'add_new', :class => 'ui-icon ui-icon-plusthick' %>
