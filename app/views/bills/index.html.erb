<% content_for :head do %>
  <script>
    var sColumns = [ 'bills.id', 'customers.name c', 'franchises.name f', 'bills.created_at' ]
    var tName = 'bill'
  </script>
<% end %>

<% table_for @bills, :html => { :class => 'data' } do |t| %>
  <%= t.data :id, :customer, :franchise, :created_at %>
<% end %>

<%= link_to 'New bill', new_bill_path, :id => 'add_new', :class => 'ui-icon ui-icon-plusthick' %>
