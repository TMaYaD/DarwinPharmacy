<% content_for :head do %>
  <script>
    var sColumns = ['product_batches.id', 'products.name', 'pack', 'batch_code', 'exp_date', 'mrp', 'vat', 'rate']
    var tName = 'product_batch'
  </script>
<% end %>

<% table_for @product_batches, :html => { :class => 'data'} do |t| %>
  <%= t.data :id, :product, :pack, :batch_code, :exp_date, :mrp, :vat, :rate %>
<% end %>

<%= link_to 'New product_batch', new_product_batch_path, :id => 'add_new', :class => 'ui-icon ui-icon-plusthick' %>
