<% semantic_form_for(@product_batch) do |f| %>
  <% f.inputs do %>
    <%= render 'inputs', :f => f %>
    <%= f.input :rate %>
    <%= f.input :vat, :as => :select, :collection => ProductBatch::VATS, :label_method => Proc.new{ |a| "#{a} %" } %>
  <% end %>
  <%= f.buttons %>
<% end %>
