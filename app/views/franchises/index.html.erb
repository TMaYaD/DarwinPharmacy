<% content_for :head do %>
  <script>
    var sColumns = [ 'franchises.id', 'name', 'address', 'users.login', 'dl', 'tin' ]
    var tName = 'franchise'
  </script>
<% end %>

<% table_for @franchises, :html => { :class => 'data' } do |t| %>
  <%= t.data :id, :name, :address, :franchisee, :dl, :tin %>
<% end %>

<%= link_to 'New franchise', new_franchise_path, :id => 'add_new', :class => 'ui-icon ui-icon-plusthick' %>
