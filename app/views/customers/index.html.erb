<% content_for :head do %>
  <script>
    sColumns = ['id', 'name', 'phone', 'address'];
    tName = 'customer';
  </script>
<% end %>
<% table_for @customers, :html => { :class => 'data' } do |t| %>
  <%= t.data :id, :name, :phone, :address %>
<% end %>

<%= link_to 'New customer', new_customer_path, :id => 'add_new', :class => 'ui-icon ui-icon-plusthick' %>
