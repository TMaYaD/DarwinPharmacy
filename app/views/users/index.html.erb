<% content_for :head do %>
  <script>
    var sColumns = [ 'id', 'login', 'email', 'phone', 'role', 'last_request_at', 'current_login_at', 'current_login_ip', 'last_login_ip' ]
    var tName = 'user';
  </script>
<% end %>

<% table_for @users, :html => { :class => 'data' } do |t| %>
  <%= t.data :id, :login, :email, :phone, :role, :last_request_at, :current_login_at, :current_login_ip, :last_login_ip %>
<% end %>

<%= link_to 'New user', new_user_path, :id => 'add_new', :class => 'ui-icon ui-icon-plusthick' %>
